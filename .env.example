# Copy this file to .env and fill in real values. Never commit .env.
TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=

# Optional: shared secret Telegram will echo back on every webhook request,
# used to reject requests that didn't actually come from Telegram.
# Generate any random string and set the same value with scripts/set-webhook.ts.
TELEGRAM_WEBHOOK_SECRET=
